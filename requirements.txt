# --- Dependencias de producción ---
Django==6.0.6
openpyxl==3.1.5           # lectura/escritura de Excel
xhtml2pdf==0.2.17         # generación de PDF desde HTML
python-decouple==3.8      # variables de entorno (.env)
whitenoise==6.11.0        # servir estáticos en producción
gunicorn==23.0.0          # servidor WSGI en producción (Linux)

# Usadas internamente por xhtml2pdf (PDF) y openpyxl (imágenes); se fijan para builds reproducibles
reportlab==4.5.1
pypdf==6.13.2
Pillow==12.2.0

# --- Dependencias de desarrollo (instalar con: pip install -r requirements-dev.txt) ---
# pytest-django==4.11.1
